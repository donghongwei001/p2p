<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dao.YpgRoleDao">	
	<select id="queryRole" resultType="java.util.HashMap">
		select * from role
	</select>
	<select id="queryPersonals" resultType="java.util.HashMap">
		select * from psersonalform
	</select>
	<select id="queryRepayMoney" resultType="java.util.HashMap">
		select r.repayid,r.projrctid,r.hktime,r.hkmoney,ptmoney,c.codename from repaymoney r,code c where r.statu=c.codeid and r.statu=8
	</select>
	<select id="queryRepay" resultType="java.util.HashMap">
		select r.repayid,r.projrctid,r.hktime,r.hkmoney,ptmoney,c.codename from repaymoney r,code c where r.statu=c.codeid and r.statu=7
	</select>
</mapper>